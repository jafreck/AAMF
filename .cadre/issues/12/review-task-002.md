```cadre-json
{
  "verdict": "pass",
  "summary": "The implementation correctly meets all acceptance criteria: `AamfOutputBase` is exported with the required fields, all 14 per-agent schemas are exported with locked `agent` literals, and `parseAamfOutput` correctly extracts the last `aamf-json` block, handles CRLF, and returns the specified result shapes for all failure modes. No bugs, security issues, or logic errors found.",
  "issues": []
}
```
