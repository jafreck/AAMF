```cadre-json
{
  "title": "Add structured aamf-json output parsing",
  "body": "## Summary\n\nEnforces a structured, schematized output contract for all agents via `aamf-json` fenced code blocks. Agents must emit their final result as an `aamf-json` block; the runtime parses and validates it against a per-agent Zod schema and marks the run as **failed** if the block is missing or invalid. Closes #12.\n\n## Changes\n\n### Runtime (`runtime/src/`)\n- **`agents/types.ts`** — Added `structuredOutput?: Record<string, unknown>`, `outputParsed: boolean`, and `parseError?: string` to `AgentResult`.\n- **`agents/result-parser.ts`** — Added `AamfOutputBase` Zod schema, 14 per-agent schemas extending it, and `ResultParser.parseAamfOutput(stdout, schema)` which extracts the **last** `aamf-json` fenced block and validates it.\n- **`core/agent-launcher.ts`** — After execution, calls `parseAamfOutput` with the agent's schema; on success populates `structuredOutput`; on missing block logs a warning and falls back to exit-code logic; on invalid block forces `success = false` and sets `parseError`. Prefers `structuredOutput.tokenUsage` over the regex-scraped value.\n- **`core/orchestrator.ts`** — Phase 4 (migration-planner) and Phase 5 (final-parity-checker) handlers now consume `structuredOutput` when `outputParsed` is `true`, falling back to legacy file/markdown parsing with a warning when not.\n\n### Agent Definitions (`.github/agents/`)\n- Added an `## Output Format` section to all 14 agent definition files (`adjudicator`, `code-migrator`, `parity-verifier`, `test-writer`, `failure-recovery`, `final-parity-checker`, `e2e-test-crafter`, `documentation-writer`, `migration-runner`, `impact-assessor`, `knowledge-builder`, `large-file-analyzer`, `migration-planner`, `migration-orchestrator`). Each section specifies the `aamf-json` block schema, a concrete valid example, and a warning that non-conforming output will be rejected by the runtime.\n\n### Tests (`runtime/tests/`)\n- **`result-parser.test.ts`** — New `parseAamfOutput` tests: valid block, last-of-multiple wins, missing block, malformed JSON, and schema validation failure.\n- **`aamf-output-schema.test.ts`** *(new)* — Valid and invalid cases for all 14 per-agent schemas.\n- **`agent-launcher.test.ts`** — New `aamf-json output parsing` describe block covering success, missing-block fallback, invalid-block failure, token precedence, and non-zero exit paths.\n- **`orchestrator.test.ts`** — New `structuredOutput Integration` describe block covering phase 3→4 structured task consumption, phase 5 structured fix consumption, empty-fix short-circuit, and `tokenUsage` precedence.\n- **`tests/helpers/mocks.ts`** — `outputParsed: false` default included in base `AgentResult` mock.\n\n## Testing\n\nAll 324 unit tests pass (`npm test` exit code 0) with no regressions. TypeScript build succeeds (`npm run build` exit code 0). The 162 E2E tests remain correctly skipped behind the `AAMF_E2E=1` gate, consistent with pre-existing behaviour.",
  "labels": ["enhancement"]
}
```
