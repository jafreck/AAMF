#!/usr/bin/env bash
set -euo pipefail

if [[ "${AAMF_SKIP_PRE_PUSH:-0}" == "1" ]]; then
  echo "[pre-push] Skipped (AAMF_SKIP_PRE_PUSH=1)"
  exit 0
fi

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[pre-push] Running local pre-push gate (ci:prepush)..."
npm run ci:prepush